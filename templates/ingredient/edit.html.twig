{% extends 'base.html.twig' %}

{% block title %}Modifier ingredient{% endblock %}

{% block body %}
    <h1>Modifier ingredient</h1>

    {{ include('ingredient/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-danger" href="{{ path('ingredient_index') }}">retour à la liste</a>

    {{ include('ingredient/_delete_form.html.twig') }}
{% endblock %}
