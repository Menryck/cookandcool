{% extends 'base.html.twig' %}

{% block title %}TableRecetteIngredients{% endblock %}

{% block body %}
<style>
.btn{
    width: 200px;
    margin: 7px;
}
</style>

    <h1>TableRecetteIngredients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table_recette_ingredient.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ table_recette_ingredient.quantite }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ table_recette_ingredient.ordre }}</td>
            </tr>
            <tr>
                <th>Etape</th>
                <td>{{ table_recette_ingredient.etape }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-danger" href="{{ path('table_recette_ingredients_index') }}">retour à la liste</a>

    <a class="btn btn-danger" href="{{ path('table_recette_ingredients_edit', {'id': table_recette_ingredient.id}) }}">modifier</a>

    {{ include('table_recette_ingredients/_delete_form.html.twig') }}
{% endblock %}
