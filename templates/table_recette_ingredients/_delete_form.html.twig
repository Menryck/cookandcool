<form method="post" action="{{ path('table_recette_ingredients_delete', {'id': table_recette_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table_recette_ingredient.id) }}">
    <button class="btn">Delete</button>
</form>
